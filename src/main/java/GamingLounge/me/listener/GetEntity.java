/*package GamingLounge.me.listener;

import org.bukkit.NamespacedKey;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;
import org.bukkit.event.entity.EntitySpawnEvent;

public class GetEntity implements Listener {


    public static final NamespacedKey ignore = new NamespacedKey("Estats", "ignore");

//Creates a Method to read entity Stats and Inventory.
    @EventHandler
    public void onLoadEntity(EntitySpawnEvent event){
//Ignores entitys that have the ignore Persistand Data Container/NBT Data and stops.
        if (event.getEntity().getPersistentDataContainer().has(ignore)) return;
        event.getEntity().get

//int currentHealth,int baseDefence


    }
public void Test(int test, float hallo){}
    

}
*/